#include <stdio.h>
int main(int argc, char *argv[]) {
	double grades2labmark(char grades[]);
	double grades2labmark(char grades[]) {	
		for (int i = 0; i < strlen(grades[]); i++) {
			if (grades[i] == 'A') {
				sum = sum + 1;
			}
			else if (grades[i] == '+') {
				sum = sum + 0.2;
			}			
		}	
	return 0;
}
